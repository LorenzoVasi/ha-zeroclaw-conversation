# Shell scripts must keep LF endings: a CRLF shebang becomes
# `#!/usr/bin/env bash\r`, which Linux reports as "bad interpreter: No
# such file or directory" — an error that names the interpreter and not
# the real cause. These are authored on Windows and run on CI's Linux
# runners, so pin them explicitly.
*.sh text eol=lf
